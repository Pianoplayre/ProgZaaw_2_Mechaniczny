<Window x:Class="HamburgerMenuDemo.AuthWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HamburgerMenuDemo"
        mc:Ignorable="d"
        Loaded="SetUpDB"
        Top="0"
        Left="0"
       
        Title="AuthWindow" Height="450" Width="700">
    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Template" >
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" CornerRadius="5" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}">
                            <Grid Margin="10,0">
                                <TextBlock x:Name="watermark" VerticalAlignment="Center" Visibility="Collapsed" FontSize="{TemplateBinding FontSize}" Opacity="0.4" Text="{TemplateBinding Tag}"></TextBlock>
                                <ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="Center"></ScrollViewer>
                                <Rectangle x:Name="rect" Fill="Transparent" Height="2" VerticalAlignment="Bottom" HorizontalAlignment="Stretch" Margin="0,3"></Rectangle>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <EventTrigger RoutedEvent="GotFocus">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Width" AutoReverse="True" From="150" To="175" Duration="0:0:0.15" >
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase ></CubicEase>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                        <DoubleAnimation Storyboard.TargetProperty="Height" AutoReverse="True" From="20" To="25" Duration="0:0:0.15" >
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase ></CubicEase>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <Trigger Property="IsFocused" Value="true">
                                <Setter TargetName="rect" Property="Fill" Value="{Binding ElementName=border, Path=BorderBrush}"></Setter>
                            </Trigger>
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="watermark" Property="Visibility" Value="Visible"></Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
        <StackPanel>
            <TextBlock Height="40" Text="Logowanie" TextAlignment="Center" FontFamily="Roboto" FontSize="26" Foreground="DimGray" Margin="0,0,0,20"/>
            <TextBlock Text="login" FontFamily="Roboto" FontSize="13" Foreground="DimGray" Margin="0,0,0,5"/>
            <TextBox x:Name="usernameInput" Width="150" Height="20" FontSize="14" Foreground="#BDB76B" BorderBrush="#247BA0" Background="#FFF7F1B2" FontFamily="Roboto" Margin="0,0,0,10"/>
            <TextBlock Text="password" FontFamily="Roboto" FontSize="13" Foreground="DimGray" Margin="0,0,0,5"/>
            <TextBox x:Name="passwordInput" Width="150" Height="20" FontSize="14" Foreground="#BDB76B" BorderBrush="#247BA0" Background="#FFF7F1B2" FontFamily="Roboto" Margin="0,0,0,10"/>
            <TextBlock Visibility="Hidden" x:Name="usernameException" Width="135" Height="20" Text="* Mandatory field" FontFamily="Roboto" FontSize="10" Foreground="DarkRed" Margin="0,0,0,10"/>
            <TextBlock Visibility="Hidden" x:Name="passwordException" Width="135" Height="20" Text="* Mandatory field" FontFamily="Roboto" FontSize="10" Foreground="DarkRed" Margin="0,0,0,20"/>
            <Button Content="Zaloguj" Width="100" Height="25" Background="Brown" Foreground="White" FontFamily="Roboto" Click="SignUpButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
